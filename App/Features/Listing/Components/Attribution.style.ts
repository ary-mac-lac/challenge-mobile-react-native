import { StyleSheet } from 'react-native'

export default () =>
  StyleSheet.create({
    text: {
      textAlign: 'center',
      lineHeight: 30,
      color: '#444',
      fontSize: 12,
      letterSpacing: 0.6,
    },
    separator: {
      width: '100%',
    },
  })
